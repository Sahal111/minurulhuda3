/**
 * Guru Wali: Data Siswa
 * Detail drawer toggle
 */

function toggleDrawer() {
    var drawer = document.getElementById('detailDrawer');
    if (drawer) drawer.classList.toggle('translate-x-full');
}
