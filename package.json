{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently -n \"frontend,backend\" -c \"blue,green\" \"npm run dev --prefix frontend\" \"cd backend && php artisan serve\"",
        "frontend": "npm run dev --prefix frontend",
        "backend": "cd backend && php artisan serve",
        "build": "npm run build --prefix frontend"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.1.0",
        "vite": "^6.0.11"
    },
    "dependencies": {
        "puppeteer": "^24.43.1"
    }
}
